const whiteListedRoutes = ['/', '/home', '/login', '/register', '/terms-and-conditions'];

const ONE_DAY = 24 * 60 * 60;

const ONE_WEEK = 7 * 24 * 60 * 60;

module.exports = {
  whiteListedRoutes,
  ONE_DAY,
  ONE_WEEK,
};
